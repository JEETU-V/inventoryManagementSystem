{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "Full-stack inventory management system with React frontend and Flask backend",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend && ./venv/bin/python app.py",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt"
  },
  "dependencies": {
    "axios": "^1.16.1",
    "html2pdf.js": "^0.14.0",
    "lucide-react": "^1.16.0",
    "pdfkit": "^0.18.0",
    "print-js": "^1.6.0",
    "react-router-dom": "^7.15.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
